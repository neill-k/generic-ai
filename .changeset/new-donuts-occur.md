---
"@generic-ai/sdk": minor
"@generic-ai/plugin-tools-terminal-sandbox": minor
---

Add sandbox network policy enforcement with `isolated`, `allowlist`, and `open` modes, including a Docker sidecar proxy for allowlisted HTTP(S) egress plus blocked-destination logging. Keep the staged file I/O policies, readonly workspace snapshots, copy-mode file bridging, generated-file metadata, and configurable staged input size caps.
