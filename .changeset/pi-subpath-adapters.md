---
"@generic-ai/core": minor
"@generic-ai/sdk": minor
---

Expose Pi runtime compatibility through the explicit `@generic-ai/sdk/pi` subpath and allow `createAgentHarness` to run registered non-Pi adapters.
