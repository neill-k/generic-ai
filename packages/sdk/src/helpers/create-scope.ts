// Re-export the canonical scope factory from the primitive module
export { createScope, createRootScope, createChildScope } from "../scope/index.js";
export type { ScopeInput, ScopeCreationOptions } from "../scope/index.js";
